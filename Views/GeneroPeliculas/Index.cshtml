@model IEnumerable<Test_Crud_Carlos_Arrieta.Models.tGeneroPelicula>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.cod_pelicula)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.cod_genero)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.cod_pelicula)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cod_genero)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.cod_genero_pelicula">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.cod_genero_pelicula">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.cod_genero_pelicula">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
