@model IEnumerable<Test_Crud_Carlos_Arrieta.Models.tRol>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.txt_desc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.sn_activo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.txt_desc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.sn_activo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.cod_rol">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.cod_rol">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.cod_rol">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
